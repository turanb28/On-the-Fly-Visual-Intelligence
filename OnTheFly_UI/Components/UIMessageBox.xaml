<Window x:Class="OnTheFly_UI.Components.UIMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OnTheFly_UI.Components"
        mc:Ignorable="d"
        Title="UIMessageBox" Height="300" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Background_Dark}"
        WindowStyle="SingleBorderWindow"
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="9*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="0.2*"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Message}" Foreground="{DynamicResource Foreground_Dark}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24"/>
        <Button Grid.Row="2" Grid.Column="1" Content="OK" Width="100" Background="{DynamicResource Background_Light}" Foreground="{DynamicResource Foreground_Dark}" BorderThickness="0" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center" Click="Button_Click"/>
    </Grid>
</Window>

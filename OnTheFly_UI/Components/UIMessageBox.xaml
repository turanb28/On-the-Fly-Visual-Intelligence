<Window x:Class="OnTheFly_UI.Components.UIMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OnTheFly_UI.Components"
        mc:Ignorable="d"
        Title="UIMessageBox" Height="300" Width="480"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource Background_Light}"
        WindowStyle="None"
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition Width="36px"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="36"></RowDefinition>
            <RowDefinition Height="0.2*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="0.2*"></RowDefinition>
        </Grid.RowDefinitions>
        <Button x:Name="close_button"  Grid.Row="0" Grid.Column="2" Content="&#x274C;" FontSize="10" Width="36" Background="Transparent" Foreground="{DynamicResource Foreground_Light}" BorderThickness="0" Height="36" HorizontalAlignment="Center" VerticalAlignment="Center" Click="close_button_Click"/>
        <Border Grid.Column="0" Grid.Row="1">
        </Border>
        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Message}" Foreground="{DynamicResource Foreground_Light }" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24"/>
        <Button Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="OK" Width="100" Background="{DynamicResource Background_Light}" Foreground="{DynamicResource Foreground_Light}" 
                BorderBrush="{DynamicResource Foreground_Light}" BorderThickness="1" Height="40" HorizontalAlignment="Center" VerticalAlignment="Center" Click="close_button_Click">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="1"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type ButtonBase}">
                                <Border x:Name="border" SnapsToDevicePixels="true" Background="Transparent" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" >
                                    <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"></ContentPresenter>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <!--<DataTrigger Binding="{Binding IsHidden}" Value="True">-->
                                        <!--<Setter TargetName="hide_icon" Property="Visibility" Value="Hidden"/>
                                    </DataTrigger>-->
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border"  Property="Background" Value="{DynamicResource Background_Dark}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="False">
                                        <Setter TargetName="border"  Property="Background" Value="{DynamicResource Background_Light}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
